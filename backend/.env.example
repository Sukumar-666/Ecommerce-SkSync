# Copy to .env and fill in real values. Never commit .env.
# MongoDB Atlas
MONGO_URI=mongodb+srv://sukumarengg:sukumarengg%40123@cluster0.vbadxem.mongodb.net/?appName=Cluster0
PORT=5000
NODE_ENV=development
# Frontend origin allowed to call this API (comma-separate multiple if needed)
CLIENT_ORIGIN=http://localhost:3000
CLIENT_URL=http://localhost:3000
# --- JWT secrets — use long random strings, DIFFERENT for each ---
JWT_ACCESS_SECRET=vUmc3LHa$VK1*^>[ca?w)_|yOG|W9IT4az3<ZPz9Sqj
JWT_REFRESH_SECRET=OsfA2)AVs9]-awZhts0_2ttxAfbOLbo=]Xb2wv[)N!;
JWT_PENDING_SECRET=obWM]?.k54NzzCoez*hT74dSHU5az-i,;2G]+R{#-[f
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_DAYS=7
# --- SMTP (used for both verification emails and OTP emails) ---
# Example for Gmail: create an "App Password" (not your normal password) at
# https://myaccount.google.com/apppasswords, then:
# SMTP_HOST=smtp.gmail.com  SMTP_PORT=587  SMTP_USER=you@gmail.com  SMTP_PASS=<app password>
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=asuransukumar123@gmail.com
SMTP_PASS=lvze dfhl dwzg tutn
SMTP_FROM="SkSync Cosmetics" <no-reply@sksync.com>
